[project]
name = "codewars"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lupus", email = "trisztanfarkas123@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
]

[project.scripts]
codewars = "codewars:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
